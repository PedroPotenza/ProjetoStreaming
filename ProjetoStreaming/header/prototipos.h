#ifndef PROTOTIPOS_H
#define PROTOTIPOS_H

int cadastro_cliente(CLIENTE* vet_cliente, int* c_cliente, int max_cliente, int c_filme_cliente);

    if(c_cliente == max_cliente)
        return 3; //numero maximo de clientes atingido
    else {

        
    }

    scanf(" %[^\n]%*c", vet_cliente[c_cliente].cliente.cliente_CPF.pessoa.nome);


#endif